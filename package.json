{
  "name": "sinocrane_guid",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . --version-string.ProductName=华鹤意居引导 --out=dist --asar --overwrite --platform=win32 --arch=x64 --icon=assets/img/app-icon/win/app.ico --ignore=node_modules/.bin --ignore=node_modules/.npminstall --ignore=node_modules/electron* --ignore=node_modules/grunt* --ignore=.git --ignore=.idea --prune=true"
  },
  "dependencies": {
    "auto-launch": "^5.0.1",
    "jquery": "^2.1.3",
    "metro-dist": "^3.0.15"
  },
  "devDependencies": {
    "electron": "^1.4.1",
    "electron-packager": "^8.1.0",
    "grunt": "^1.0.1",
    "grunt-electron-installer": "^2.1.0"
  }
}