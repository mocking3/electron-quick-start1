{
  "name": "huahe-guid",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . --out=dist --overwrite --platform=win32 --arch=x64 --icon=assets/app-icon/win/app.ico --ignore=node_modules/.bin --ignore=.git --ignore=.idea --ignore=dist --prune=true"
  },
  "dependencies": {
    "jquery": "^3.1.1",
    "metro-dist": "^3.0.15"
  },
  "devDependencies": {
    "electron": "^1.4.1",
    "electron-packager": "^8.1.0"
  }
}